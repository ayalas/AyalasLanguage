{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "admin/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "dotnet#build": {
      "outputs": ["bin/**", "obj/**"]
    }
  }
}